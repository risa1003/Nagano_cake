<h2>商品詳細</h2>

<div>
  <%= image_tag @item.image, size: "400x300" %>
  <p>商品名 <%= @item.name %></p>
  <p>商品説明 <%= @item.introduction %></p>
  <p>ジャンル <%= @item.genre.name %></p>
  <p>税込価格(税抜価格) <%= @item.price_with_tax %>(<%= @item.price %>)円</p>
  <p>販売ステータス</p><p class="<%= @item.is_sold ? 'text-success' : 'text-secondary' %> font-weight-bold"><%= @item.is_sold ? '販売中' : '販売停止中' %></p>
  <%= link_to "編集する", edit_admin_item_path, class: "btn btn-success" %>
</div>