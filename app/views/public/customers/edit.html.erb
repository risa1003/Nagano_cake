<h2>会員情報編集</h2>
<%= form_with model: @customer, url: edit_customer_path do |f| %>
  <%= render "public/shared/error_messages", resource: resource %>
  
  <div class="field">
    <%= f.label :
  </div>
<% end %>




  <div class="field">
    <%= f.label :last_name, "(姓)" %>
    <%= f.text_field :last_name %>
  </div>

<%= form_with model: @user do |f| %>
  <div class="form-group">
    <%= f.label :full_name, "名前" %>
    <div class="row">
      <div class="col-md-6">
        <%= f.text_field :last_name, class: "form-control", placeholder: "姓 (Last Name)" %>
      </div>
      <div class="col-md-6">
        <%= f.text_field :first_name, class: "form-control", placeholder: "名 (First Name)" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :phone_number, "電話番号" %>
    <%= f.text_field :phone_number, class: "form-control", placeholder: "電話番号" %>
  </div>
<% end %>
